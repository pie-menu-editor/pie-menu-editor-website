:root {
  color-scheme: light;
  --background: #cbcbc7;
  --text: #181818;
  --muted: #525250;
  --line: #bdbdb8;
  --control-line: #686864;
  --accent: #972c36;
  --accent-hover: #842731;
  --link: #171c52;
  --content-width: 680px;
}

* { box-sizing: border-box; }

html {
  min-height: 100%;
  font-family: "IBM Plex Sans", "Helvetica Neue", Arial, sans-serif;
  line-height: 1.6;
  background:
    radial-gradient(circle at 50% 12%, #fdfdfb 0, #f1f1ee 23rem, #e2e2de 45rem, transparent 69rem),
    var(--background);
  color: var(--text);
}

body { min-height: 100vh; margin: 0; }

a {
  color: var(--link);
  text-decoration-color: currentcolor;
  text-decoration-thickness: 0.08em;
  text-underline-offset: 0.2em;
}

a:hover { color: var(--accent-hover); }

a:focus-visible,
button:focus-visible,
input:focus-visible,
select:focus-visible {
  outline: 3px solid var(--link);
  outline-offset: 3px;
}

.wrap {
  width: min(calc(100% - 2.5rem), var(--content-width));
  margin-inline: auto;
}

header {
  border-bottom: 1px solid var(--line);
  background: rgb(255 255 255 / 18%);
}

header .wrap {
  display: flex;
  align-items: center;
  justify-content: space-between;
  min-height: 3.75rem;
  gap: 1rem;
}

.brand { color: var(--text); font-weight: 700; text-decoration: none; }
.support-link { font-size: 0.95rem; }
main { padding-block: clamp(2.5rem, 6vw, 4rem); }

.eyebrow {
  margin: 0 0 0.55rem;
  color: var(--accent);
  font-size: 0.85rem;
  font-weight: 700;
  letter-spacing: 0.12em;
  text-transform: uppercase;
}

h1 { margin: 0; font-size: clamp(2rem, 5vw, 2.8rem); line-height: 1.1; letter-spacing: -0.035em; }
h2 { margin: 0 0 0.5rem; font-size: 1.05rem; }
h3 { margin: 0; font-size: 0.98rem; }
.intro { max-width: 620px; margin: 0.85rem 0 0; color: var(--muted); }

.task-selector {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 0.55rem;
  margin-top: 1.75rem;
}

.task-button {
  min-height: 2.65rem;
  padding: 0.5rem 0.75rem;
  border-color: var(--control-line);
  background: rgb(255 255 255 / 24%);
  color: var(--text);
  text-align: left;
}

.task-button[aria-current="page"] {
  border-color: var(--text);
  background: var(--text);
  color: #fff;
}

.setup-card,
.credentials,
.result {
  margin-top: 2rem;
  padding-block: 1.5rem;
  border-block: 1px solid var(--line);
}

.setup-card > p,
.pending > p { margin: 0; color: var(--muted); }

.setup-preview {
  margin-top: 1rem;
  padding: 0.8rem 1rem;
  border-left: 3px solid var(--link);
  background: rgb(255 255 255 / 24%);
}

.setup-preview strong { display: block; }
.setup-preview p { margin: 0.15rem 0 0; color: var(--muted); }

.field-group { margin-top: 1.1rem; }
.primary-action { margin-top: 1.15rem; }
label,
.field-label { display: block; margin-bottom: 0.45rem; font-weight: 700; }

.field-row,
.value-row {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  gap: 0.65rem;
}

input,
select,
button,
code {
  min-height: 2.8rem;
  border: 1px solid var(--control-line);
  border-radius: 0.2rem;
  font: inherit;
}

input,
select {
  width: 100%;
  padding: 0.65rem 0.8rem;
  background: rgb(255 255 255 / 68%);
  color: var(--text);
}

input::placeholder { color: #686864; opacity: 1; }

button {
  padding: 0.65rem 1.15rem;
  border-color: var(--text);
  background: var(--text);
  color: #fff;
  font-weight: 700;
  cursor: pointer;
}

button:hover:not(:disabled) { border-color: #343434; background: #343434; }
button:disabled { cursor: not-allowed; opacity: 0.55; }
button.secondary { border-color: var(--control-line); background: transparent; color: var(--text); }
button.quiet { border-color: transparent; background: transparent; color: var(--link); }

.field-help { margin: 0.55rem 0 0; color: var(--muted); font-size: 0.9rem; }

.pending,
.message {
  margin-top: 1rem;
  padding: 0.85rem 1rem;
  border-left: 3px solid var(--link);
  background: rgb(255 255 255 / 26%);
}

.pending label { margin-top: 0.75rem; }
.small-actions { display: flex; flex-wrap: wrap; gap: 0.5rem; margin-top: 0.75rem; }
.small-actions button { min-height: 2.4rem; padding: 0.4rem 0.7rem; font-size: 0.9rem; }

.message[data-kind="error"] { border-left-color: var(--accent); }
.message[data-kind="success"] { border-left-color: #2f6450; }
.message strong { display: block; }
.message p { margin: 0.15rem 0 0; color: var(--muted); }

.credentials > h2 { font-size: 1.35rem; }

.save-warning {
  display: grid;
  grid-template-columns: auto minmax(0, 1fr);
  gap: 0.9rem;
  margin-top: 1rem;
  padding: 1rem;
  border: 1px solid var(--accent);
  background: rgb(255 255 255 / 34%);
}

.save-warning p { margin: 0.15rem 0 0; color: var(--muted); }
.warning-mark {
  display: grid;
  width: 2rem;
  height: 2rem;
  place-items: center;
  border-radius: 50%;
  background: var(--accent);
  color: #fff;
  font-size: 1.25rem;
  font-weight: 700;
  line-height: 1;
}

.credential {
  margin-top: 1rem;
  padding: 1rem;
  border: 1px solid var(--line);
  background: rgb(255 255 255 / 22%);
}

.credential-heading {
  display: grid;
  grid-template-columns: auto minmax(0, 1fr) auto;
  gap: 0.75rem;
  align-items: center;
}

.credential-number {
  display: grid;
  width: 2rem;
  height: 2rem;
  place-items: center;
  border: 1px solid var(--text);
  border-radius: 50%;
  font-weight: 700;
  line-height: 1;
}

.credential-title-group { min-width: 0; }
.credential-title-group .field-label { margin: 0; }
.destination-label {
  display: block;
  margin-top: 0.05rem;
  color: var(--muted);
  font-size: 0.86rem;
}

.security-label {
  padding: 0.2rem 0.45rem;
  border: 1px solid currentcolor;
  border-radius: 999px;
  font-size: 0.72rem;
  font-weight: 700;
  letter-spacing: 0.06em;
  line-height: 1.3;
  text-transform: uppercase;
}

.secret-label { color: var(--accent); }
.credential .value-row { margin-top: 0.85rem; }

.result > p { margin: 0; color: var(--muted); }
.result-details { margin: 1rem 0 0; }
.result-details > div {
  display: grid;
  grid-template-columns: minmax(7rem, 0.35fr) 1fr;
  gap: 1rem;
  padding-block: 0.55rem;
  border-top: 1px solid var(--line);
}
.result-details dt { color: var(--muted); }
.result-details dd { margin: 0; font-weight: 700; }
code {
  display: flex;
  align-items: center;
  min-width: 0;
  padding: 0.6rem 0.75rem;
  overflow-wrap: anywhere;
  background: rgb(255 255 255 / 52%);
  font-family: "IBM Plex Mono", Consolas, monospace;
  font-size: 0.9rem;
}

.acknowledge { margin-top: 1.5rem; }
.confirmation { display: flex; align-items: flex-start; gap: 0.55rem; font-weight: 400; }
.confirmation input { width: 1.1rem; min-height: 1.1rem; margin-top: 0.25rem; }
.acknowledge > button { margin-top: 0.75rem; }
.next { padding-block: 2rem 2.5rem; }
.next-steps { margin: 0.9rem 0 0; padding-left: 1.3rem; }
.next-steps li + li { margin-top: 0.35rem; }
.next-action {
  display: flex;
  width: fit-content;
  align-items: center;
  justify-content: space-between;
  gap: 2.5rem;
  margin-top: 1rem;
  padding: 0.75rem 1rem;
  border: 1px solid var(--text);
  border-radius: 0.2rem;
  background: var(--text);
  color: #fff;
  font-weight: 700;
  text-decoration: none;
}
.next-action:hover { background: #343434; color: #fff; }
.next-action span { font-size: 1.2rem; }

footer {
  padding-block: 2rem 3rem;
  border-top: 1px solid var(--line);
  color: var(--muted);
  font-size: 0.9rem;
}

[hidden] { display: none !important; }

@media (max-width: 640px) {
  .task-selector { grid-template-columns: 1fr; }
  .field-row,
  .value-row { grid-template-columns: 1fr; }
  .credential-heading { grid-template-columns: auto minmax(0, 1fr); }
  .security-label { grid-column: 2; justify-self: start; }
  .next-action { width: 100%; }
  .field-row button,
  .value-row button,
  .acknowledge > button { width: 100%; }
}
